"Colors
syntax enable "enable syntax highlighting
set background=dark "use the dark color scheme
colorscheme solarized "use solarized

"Tabs and spaces
set tabstop=2 "number of visual spaces per tab
set softtabstop=2 "number of spaces inserted upon hitting tab
set smarttab
set expandtab "tabs are spaces
set si "smart indent

"UI config
set number "show line numbers
set showcmd "show command in bottom bar
set cursorline "highlight current line
set wildmenu "visual autocomplete for file menu
set showmatch "highlight matching brackets and parens
set wrap "wrap lines

"Search
set incsearch "search as characters are entered
set hlsearch "highlight matches
set ignorecase "ignore case when searching
set smartcase
  
"Encoding
set encoding=utf8

"Misc
set nobackup "prevent cluttering ~file

